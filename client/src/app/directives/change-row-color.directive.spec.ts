import { ChangeRowColorDirective } from './change-row-color.directive';

describe('ChangeRowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeRowColorDirective();
    expect(directive).toBeTruthy();
  });
});
